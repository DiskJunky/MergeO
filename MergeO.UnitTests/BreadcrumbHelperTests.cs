namespace MergeO.Tests
{
    using NUnit.Framework;

    [TestFixture]
    public class BreadcrumbHelperTests
    {
        [Test]
        public static void ToBreadcrumb_GetsValidBreadcrumb()
        {
            //string expected = typeof(model).Name
            //                    .Append(nameof(Model.Prop1))
            //                    .Append(nameof(Model.Prop2));
            //string actual = BreadcrumbHelper<Model>.Of(a => a.Prop1.Prop2);
            //Assert.AreEqual(expected, actual);
        }
    }
}
