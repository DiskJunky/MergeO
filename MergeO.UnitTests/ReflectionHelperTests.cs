namespace MergeO.UnitTests
{
    using NUnit.Framework;

    [TestFixture]
    public static class ReflectionHelperTests
    {
        [Test]
        public static void GetPropertyName_Valid_Returns_Property()
        {
            //string expected = nameof(Model.ModelID);
            //string value = ReflectionHelper.GetPropertyName<Model, int>(m => m.ModelID);
            //Assert.AreEqual(expected, value);
        }
    }
}
